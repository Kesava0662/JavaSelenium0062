FROM maven:3.8.6-eclipse-temurin-11

# Install Chrome and ChromeDriver (already done properly)

# Copy project
COPY . /project
WORKDIR /project

# Build and run tests
RUN mvn clean install

# Optional entrypoint
CMD ["java", "-cp", "target/classes:target/test-classes:target/dependency/*", "runner.MainApp"]
